import { Canvas, Meta } from "@storybook/addon-docs/blocks";
import * as ButtonStories from "../src/components/Button.stories.jsx";

<Meta title="Components/Button/Usage" />

# Button

A simple button for triggering an action.

Use `title` for its label and `onPress` for the action to run when it is pressed.

<Canvas of={ButtonStories.Default} />
